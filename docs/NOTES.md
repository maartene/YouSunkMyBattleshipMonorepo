# NOTES

## TODO
- Decide on how to get events from other (CPU player): polling, WS or a custom NIO protocol?

## DOING

## DONE
- Migrate the iOS app to use a cells property instead of a function. This makes keeping the view in sync with the model much easier. The 'force update' can also go.
