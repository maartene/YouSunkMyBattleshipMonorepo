version: '3.8'
services:
  database:
    build:
      context: ./YouSunkMyBattleshipBE/infrastructure/database
      dockerfile: Dockerfile
    image: yousunkmybattleship_database:latest
    restart: unless-stopped
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

  service:
    build:
      context: .
      dockerfile: ./YouSunkMyBattleshipBE/infrastructure/service/Dockerfile
    image: yousunkmybattleship_service:latest
    restart: "no"
    ports:
      - "8080:8080"
    depends_on:
      - database

volumes:
  db_data:
    driver: local
