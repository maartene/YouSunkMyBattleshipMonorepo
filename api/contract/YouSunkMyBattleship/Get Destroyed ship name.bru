meta {
  name: Get Destroyed ship name
  type: http
  seq: 6
}

get {
  url: http://localhost:8080/shipAt/I6
  body: none
  auth: inherit
}

script:pre-request {
  const requestResponse = await bru.runRequest(
    "Destroy ship"
  );
}

tests {
  test("should return the name of the destroyed ship", () => {
    const body = res.getBody()
    expect(body).to.eq("Destroyer")
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
